import styled from "styled-components";

export const Title = styled.h1`
  color: var(--text-color);
  font-size: 26px;
  font-weight: 600;
  line-height: 140%;
  text-align: center;

  margin-top: 16px;
  margin-bottom: 24px;
`;
